#include<bits/stdc++.h>
using namespace std;

int rec(int arr[], int n, int &k) {
    sum = max()
}

int solve(int arr[], int n) {
    int k = 1;
    int ans = rec(arr, n, k);
    return ans;
}

int main() {
    int n;
    cin >> n;
    int arr[n];
    for(int i=0; i<n; i++) {
        cin >> arr[i];
    }
    cout << solve(arr, n);
    return 0;
}
